# Legacy Folder

This folder contains archived assets that are not part of the current runtime path.

## Contents

- `report-ui/`: legacy componentized dashboard modules formerly located at the repo root.

These files are kept for reference and historical context while active application code lives under `app/`, `lib/`, and `public/`.
